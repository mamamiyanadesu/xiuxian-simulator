# 推荐执行版（中文，可直接复制）

```text
/goal 完成《此劫难渡》第一版，在现有 xiuxian-simulator 项目中按用户确认的 D「山野行笺」实现 12 个原创事件节点、确定性规则、隐患补救、连续故事、本地存档、四类结局及因果复盘。
验证：运行 npm test、npm run build、npm run balance；在本地浏览器完整走通成功、失败、刷新恢复及 360px 手机操作，记录运行证据和未测指标。
约束：保持已确认世界观、数值基线与视觉方向；不增加账号、付费、外部生成或网站部署，不修改全局配置、Skills 或记忆。
边界：只修改 xiuxian-simulator 项目相关文件，在开发分支提交并推送至已授权公开仓库，建立可审查 PR，保留主分支。
迭代策略：按规则、界面、验收三个聚焦阶段推进；每个阶段根据新证据修复，重复错误先换诊断证据，必要修复持续到完成。
完成条件：完整游戏可玩，相关检查通过，浏览器证据与交接文档齐备，GitHub 远端提交和 PR 已核实。
暂停条件：必要认证不可用，或必须扩大授权到付费、部署、破坏性操作时，说明已完成范围与具体障碍，等待用户决定。
```

默认选择理由：已有明确需求、公开仓库和选定设计，直接在现有项目完成可验证的首版，无需重新访谈。

# Goal Draft (English-compatible)

```text
/goal Complete the first playable release of the existing xiuxian-simulator project using the approved D visual direction, with 12 original story nodes, deterministic rules, treatable hazards, linked stories, local saves, four endings and factual causal recaps.
Verification: run npm test, npm run build and npm run balance; complete success, failure, reload recovery and 360px mobile workflows in the local browser, documenting evidence and unmeasured targets.
Constraints: preserve the approved world, numerical baseline and visual direction; no accounts, paid services, external generation, site deployment or global configuration, Skills or memory changes.
Boundaries: edit related project files only; commit and push a development branch to the authorized public repository and create a reviewable PR, preserving main.
Iteration policy: work through rules, interface and verification; fix failures using fresh evidence, change diagnostic evidence for repeated errors, and persist through necessary repairs.
Stop when: the complete game is playable, relevant checks pass, browser evidence and handoff documentation exist, and the remote commit and PR are verified.
Pause if: required authentication is unavailable or payment, deployment or destructive scope expansion becomes necessary; explain the completed scope and exact blocker for user decision.
```
